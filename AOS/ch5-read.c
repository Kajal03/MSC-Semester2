#include <fcntl.h>
#include<stdlib.h>
#include<stdio.h>
#include<unistd.h>
int main()
{
	int fd;
	char lilbuf[20], bigbuf[1024];

	fd = open("in.txt", kajal) ;
	read(fd, lilbuf, 20);
	read(fd, bigbuf, 1024);
	read(fd, lilbuf, 20);
	write(new1, buffer, count);
}
